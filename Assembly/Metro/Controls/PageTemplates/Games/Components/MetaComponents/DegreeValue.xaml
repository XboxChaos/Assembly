<UserControl x:Class="Assembly.Metro.Controls.PageTemplates.Games.Components.MetaComponents.DegreeValue"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="35" Width="580"
             HorizontalAlignment="Left">
	<Grid>
		<StackPanel Orientation="Horizontal">
			<TextBlock x:Name="lblValueName" Text="{Binding Name}" ToolTip="{Binding Name}" Style="{DynamicResource GenericTextblock}" Width="180" VerticalAlignment="Center" Margin="0,0,0,0" TextTrimming="CharacterEllipsis"/>
			<TextBox x:Name="txtValue" Text="{Binding Degree}" Height="24" VerticalAlignment="Center" Width="200" Padding="0,0,0,0" Margin="20,0,0,0" />
			<TextBlock x:Name="lblType" Text="degree" Style="{DynamicResource GenericTextblock}" VerticalAlignment="Center" Width="150" Margin="20,0,0,0" />
		</StackPanel>
	</Grid>
</UserControl>
