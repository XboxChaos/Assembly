<UserControl x:Class="Atlas.Pages.CacheEditors.AdvancedMemoryEditor"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:models="clr-namespace:Atlas.Models"
			 mc:Ignorable="d" 
			 d:DesignHeight="600" d:DesignWidth="800">
	<Grid>
		<DataGrid x:Name="MemoryDataGrid" Background="{x:Null}" ItemsSource="{Binding (models:EngineMemory+EngineVersion.MemoryValues)}"
				  VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Standard"
				  AutoGenerateColumns="True" GridLinesVisibility="None" RowHeaderWidth="0" HeadersVisibility="Column" CanUserResizeColumns="True">
			<!--<DataGrid.Columns>
				<DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="3*" />
				<DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="3*" />
			</DataGrid.Columns>-->
		</DataGrid>
	</Grid>
</UserControl>
