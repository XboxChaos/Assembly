<UserControl x:Class="Assembly.Metro.Controls.PageTemplates.Games.Components.MetaComponents.rawBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:local="clr-namespace:Assembly.Metro.Controls.PageTemplates.Games.Components.MetaComponents"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
			 xmlns:mvvmTextEditor="clr-namespace:Assembly.Metro.Controls.BindingTextEditor"
             xmlns:Components="clr-namespace:Assembly.Metro.Controls.PageTemplates.Games.Components" mc:Ignorable="d" Height="185" Width="595"
             HorizontalAlignment="Left">
    <UserControl.Resources>
        <local:LowercaseConverter x:Key="lowercase" />
		<Components:HexValueConverter x:Key="IValueHexValueConverter" />
    </UserControl.Resources>
    <Grid>
        <TextBlock x:Name="lblValueName" Text="{Binding Name}" ToolTip="{Binding Name}" Style="{DynamicResource GenericTextblock}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,5,0,0" Width="180" TextTrimming="CharacterEllipsis"/>
        <Border BorderBrush="#595959" BorderThickness="1" Margin="200,5,10,0" VerticalAlignment="Top" Height="135">
			<avalonEdit:TextEditor	x:Name="txtValue" ShowLineNumbers="False"
									Document="{Binding Path=Document}" Padding="4,2,2,2" 
									Background="#414141" Foreground="White" 
									FontFamily="Consolas" MouseRightButtonDown="txtValue_MouseRightButtonDown_1" TextChanged="txtValue_TextChanged"
									FontSize="10pt">
				<avalonEdit:TextEditor.ContextMenu>
					<ContextMenu>
						<MenuItem Name="cutItem" Command="Cut" />
						<MenuItem Name="copyItem" Command="Copy" />
						<MenuItem Name="pasteItem" Command="Paste" />
					</ContextMenu>
				</avalonEdit:TextEditor.ContextMenu>
			</avalonEdit:TextEditor>
			
			<!--<mvvmTextEditor:MvvmTextEditor x:Name="txtValue" Padding="4,2,2,2" HorizontalAlignment="Stretch" VerticalScrollBarVisibility="Auto" VerticalAlignment="Stretch"
                                   FontFamily="Consolas" FontSize="10pt" Background="#414141" Foreground="White" MouseRightButtonDown="txtValue_MouseRightButtonDown_1">
				<mvvmTextEditor:MvvmTextEditor.DocumentText>
					<Binding Path ="Value" Mode="TwoWay" 
							 UpdateSourceTrigger="PropertyChanged">
					</Binding>
				</mvvmTextEditor:MvvmTextEditor.DocumentText>
				<mvvmTextEditor:MvvmTextEditor.ContextMenu>
                    <ContextMenu>
                        <MenuItem Name="cutItem" Command="Cut" />
                        <MenuItem Name="copyItem" Command="Copy" />
                        <MenuItem Name="pasteItem" Command="Paste" />
                    </ContextMenu>
				</mvvmTextEditor:MvvmTextEditor.ContextMenu>
			</mvvmTextEditor:MvvmTextEditor>-->
        </Border>

		<Grid VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="385" Margin="0,0,10,9">
			<StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
				<TextBlock Text="Length: " Style="{DynamicResource GenericTextblock}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,5,0,0" Width="50" TextTrimming="CharacterEllipsis"/>
				<TextBox Text="{Binding Length}" Width="138" IsReadOnly="True"/>
			</StackPanel>

			<StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
				<TextBlock Text="Offset: " Style="{DynamicResource GenericTextblock}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,5,0,0" Width="50" TextTrimming="CharacterEllipsis"/>
				<TextBox Text="{Binding Path=FieldAddress, Converter={StaticResource IValueHexValueConverter}}" Width="138" IsReadOnly="True"/>
			</StackPanel>
		</Grid>

	</Grid>
</UserControl>
